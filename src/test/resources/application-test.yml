spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

api:
  gateway:
    cors:
      allowed-origins: http://test.localhost
